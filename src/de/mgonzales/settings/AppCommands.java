package de.mgonzales.settings;

public class AppCommands {
    //region Konstanten
    // Bereich für Konstantendeklarationen
    // Konstante, die den Befehl zum Erstellen eines neuen Spiels repräsentiert
    public static final int CREATE = 1;

    // Konstante, die den Befehl zum Anzeigen der Spieleliste repräsentiert
    public static final int SHOW = 2;

    // Konstante, die den Befehl zum Beenden der App repräsentiert
    public static final int EXIT = 0;
    //endregion Konstanten

    //region Attribute
    // Bereich für Attributdeklarationen
    //endregion Attribute

    //region Konstruktoren
    // Bereich für Konstruktoren (aktuell leer, da es sich um eine Klasse mit statischen Attributen handelt)
    //endregion Konstruktoren

    //region Methoden
    // Bereich für Methoden (aktuell leer, da keine Methoden definiert sind)
    //endregion Methoden
}
